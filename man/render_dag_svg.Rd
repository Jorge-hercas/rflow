% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{render_dag_svg}
\alias{render_dag_svg}
\title{Render a DAG's dependency graph as a self-contained SVG string}
\usage{
render_dag_svg(
  dag,
  states = NULL,
  node_w = 148,
  node_h = 40,
  col_gap = 220,
  row_gap = 64
)
}
\arguments{
\item{dag}{A [DAG] object.}

\item{states}{Optional named character vector: task_id -> one of
"success"/"failed"/"running"/"skipped"/"pending".}

\item{node_w, node_h}{Node box dimensions in px.}

\item{col_gap, row_gap}{Spacing between layers/rows in px.}
}
\value{
A character string of raw SVG markup.
}
\description{
Lays tasks out left-to-right in topological "layers" (stages), connecting
each task to its downstream tasks with elbow connectors — a transit-map
style rendering. If `states` is supplied (a named character vector,
task_id -> state), nodes are colored by run state; otherwise all nodes are
drawn in a neutral "defined" color.
}
