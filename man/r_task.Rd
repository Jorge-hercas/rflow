% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task.R
\name{r_task}
\alias{r_task}
\title{Define a task (an operator instance) inside the currently active DAG}
\usage{
r_task(
  task_id,
  func,
  op_args = list(),
  retries = 0,
  retry_delay = 5,
  timeout = NULL,
  trigger_rule = "all_success",
  doc = NULL,
  dag = rflow_active_dag()
)
}
\arguments{
\item{task_id}{Unique identifier for the task within its DAG.}

\item{func}{An R function to execute. It may accept named arguments matching
`op_args`, and may optionally declare a `context` parameter to receive
scheduling metadata (execution_date, ds, dag_id, task_id, run_id, ti — a
list of upstream return values keyed by task_id, i.e. R's answer to
Airflow's XCom).}

\item{op_args}{A named list of arguments passed to `func`.}

\item{retries}{Number of retries on failure (default 0).}

\item{retry_delay}{Seconds to wait between retries (default 5).}

\item{timeout}{Optional per-task timeout in seconds.}

\item{trigger_rule}{One of "all_success" (default), "all_done", "one_success",
"one_failed", "none_failed".}

\item{doc}{Optional free-text documentation for the task.}

\item{dag}{The DAG to attach to. Defaults to the currently active DAG set by
`with_dag()`.}
}
\value{
The created [Task] object (invisibly usable with `%>>%` chaining).
}
\description{
This is the primary way to define work in rflow, analogous to instantiating
a `PythonOperator` in Apache Airflow. Must be called inside a `with_dag()`
block, or with an explicit `dag =` argument.
}
