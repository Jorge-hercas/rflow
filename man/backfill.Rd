% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/executor.R
\name{backfill}
\alias{backfill}
\title{Backfill a DAG over a range of execution dates}
\usage{
backfill(
  dag,
  start_date,
  end_date = Sys.time(),
  executor = "sequential",
  con = NULL,
  verbose = TRUE
)
}
\arguments{
\item{dag}{A [DAG] object.}

\item{start_date}{first execution date (inclusive)}

\item{end_date}{last execution date (inclusive)}

\item{executor}{"sequential" (default) or "future" for layer-parallel
execution via the future package (configure a plan with
`future::plan(future::multisession)` beforehand for real parallelism).}

\item{con}{Optional DBI connection from [rflow_db_connect()] to persist run
and task_instance state. If NULL, runs in-memory only.}

\item{verbose}{Print a live progress summary to the console (default TRUE).}
}
\value{
An invisible list of per-run results as returned by [run_dag()].
}
\description{
Analogous to `airflow dags backfill`. Triggers one run per scheduled
interval between `start_date` and `end_date`.
}
