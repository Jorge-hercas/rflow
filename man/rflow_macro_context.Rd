% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{rflow_macro_context}
\alias{rflow_macro_context}
\title{Build the templating context for a given execution date}
\usage{
rflow_macro_context(
  execution_date,
  dag_id = NA,
  task_id = NA,
  run_id = NA,
  extra = list()
)
}
\arguments{
\item{execution_date}{A Date/POSIXct/parseable string.}

\item{dag_id, task_id, run_id}{Identifiers to embed in the context.}

\item{extra}{A named list merged in on top (e.g. `list(ti = results)`).}
}
\value{
A named list.
}
\description{
Produces the named list (`ds`, `ds_nodash`, `ts`, `execution_date`, `dag_id`,
`task_id`, `run_id`, plus anything in `extra` such as `ti`) that is passed
as `context` into every task function, and used by [rflow_render()].
}
