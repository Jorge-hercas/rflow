% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui.R
\name{rflow_ui}
\alias{rflow_ui}
\title{Launch the rflow interactive dashboard (the "webserver")}
\usage{
rflow_ui(dags = NULL, con = rflow_db_connect("rflow.db"), ...)
}
\arguments{
\item{dags}{A list of [DAG] objects to expose (defaults to every DAG
registered in the current session via [list_dags()]).}

\item{con}{A DBI connection from [rflow_db_connect()]. Defaults to a fresh
on-disk `rflow.db` in the working directory.}

\item{...}{Passed through to `shiny::runApp()` (e.g. `port =`, `launch.browser =`).}
}
\value{
Does not return; runs the Shiny app (blocking) or, in non-interactive
  test contexts, returns the `shiny.appobj` invisibly without launching it.
}
\description{
A Shiny control-room dashboard for browsing registered DAGs, inspecting
their dependency graph, triggering runs, and reviewing run history and
per-task logs -- analogous to the Airflow webserver, entirely in R.
}
