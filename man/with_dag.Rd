% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dag.R
\name{with_dag}
\alias{with_dag}
\title{Create a DAG and make it the "active" DAG for the duration of `code`}
\usage{
with_dag(dag, code)
}
\arguments{
\item{dag}{Either a [DAG] object, or arguments are passed to `DAG$new()` if
a dag_id string is given as the first argument via `...`.}

\item{code}{An expression (typically a `{ ... }` block) to evaluate with the
DAG active.}
}
\value{
The [DAG] object, invisibly.
}
\description{
Mirrors Airflow's `with DAG(...) as dag:` context manager. Any [r_task()]
calls inside `code` that don't specify `dag=` explicitly attach to this DAG.
}
\examples{
\dontrun{
dag <- with_dag(DAG$new("my_dag", schedule_interval = "@daily"), {
  t1 <- r_task("extract", function() 1)
  t2 <- r_task("load", function(context) context$ti$extract + 1)
  t1 \%>>\% t2
})
}
}
