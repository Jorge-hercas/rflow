% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/executor.R
\name{run_dag}
\alias{run_dag}
\title{Execute a DAG once (a "DAG run"), analogous to `airflow dags trigger`}
\usage{
run_dag(
  dag,
  execution_date = Sys.time(),
  executor = c("sequential", "future"),
  con = NULL,
  run_id = NULL,
  conf = list(),
  verbose = TRUE,
  run_type = "manual"
)
}
\arguments{
\item{dag}{A [DAG] object.}

\item{execution_date}{The logical execution date/time for this run
(defaults to now). Used to populate `{{ ds }}`-style template macros.}

\item{executor}{"sequential" (default) or "future" for layer-parallel
execution via the future package (configure a plan with
`future::plan(future::multisession)` beforehand for real parallelism).}

\item{con}{Optional DBI connection from [rflow_db_connect()] to persist run
and task_instance state. If NULL, runs in-memory only.}

\item{run_id}{Optional explicit run id; auto-generated (UUID) if omitted.}

\item{conf}{Optional named list of extra config, merged into the template
context and stored alongside the dag_run row.}

\item{verbose}{Print a live progress summary to the console (default TRUE).}
}
\value{
An invisible list with `run_id`, `state` ("success"/"failed"),
  `results` (named list of task return values), and `task_instances`
  (a data.frame summary of every task's outcome).
}
\description{
Runs every task in dependency order, honoring retries, `trigger_rule`s, and
persisting state to the metadata DB if `con` is supplied. Task return
values are collected into `context$ti`, a named list keyed by `task_id` —
R's answer to Airflow's XCom — so downstream tasks can read upstream
results simply as `context$ti$upstream_task_id`.
}
